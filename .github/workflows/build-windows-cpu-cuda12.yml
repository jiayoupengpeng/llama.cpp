name: Build Windows CPU + CUDA12 (Final Stable No External CUDA Action)
on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - '.github/workflows/build-windows-cpu-cuda12.yml'
      - '**/CMakeLists.txt'
      - '**/.cmake'
      - '**/*.h'
      - '**/*.hpp'
      - '**/*.c'
      - '**/*.cpp'

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref && github.ref || github.run_id }}
  cancel-in-progress: true

env:
  GGML_NLOOP: 3
  GGML_N_THREADS: 1
  LLAMA_ARG_LOG_COLORS: 1
  LLAMA_ARG_LOG_PREFIX: 1
  LLAMA_ARG_LOG_TIMESTAMPS: 1

jobs:
  build-cpu-x64:
    runs-on: windows-2022
    steps:
      - name: Clone source code
        uses: actions/checkout@v6

      - name: ccache compile cache speedup
        uses: ggml-org/ccache-action@v1.2.21
        with:
          key: cpu-windows-x64-static
          evict-old-files: 1d
          save: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' }}

      - name: CMake Build CPU (Enable llama-server & full MCP)
        run: |
          Remove-Item -Recurse -Force build-cpu -ErrorAction SilentlyContinue
          cmake -S . -B build-cpu ^
            -A x64 ^
            -DGGML_NATIVE=OFF ^
            -DLLAMA_BUILD_SERVER=ON ^
            -DLLAMA_BUILD_BORINGSSL=ON ^
            -DGGML_RPC=ON ^
            -DGGML_BACKEND_DL=ON ^
            -DGGML_CPU_ALL_VARIANTS=ON
          cmake --build build-cpu --config Release -j %NUMBER_OF_PROCESSORS%

      - name: CTest basic functional test
        run: |
          cd build-cpu
          ctest -L main -C Release --verbose --timeout 900

      - name: Package CPU release zip
        run: |
          mkdir artifact-cpu
          Copy-Item build-cpu/Release/* artifact-cpu/ -Recurse
          Compress-Archive -Path artifact-cpu/* -DestinationPath llama-win-cpu-mcp.zip

      - name: Upload CPU build artifact
        uses: actions/upload-artifact@v4
        with:
          name: llama-win-cpu-mcp
          path: llama-win-cpu-mcp.zip

  build-cuda12-x64:
    runs-on: windows-2022
    steps:
      - name: Clone source code
        uses: actions/checkout@v6

      - name: ccache compile cache speedup
        uses: ggml-org/ccache-action@v1.2.21
        with:
          key: cuda-windows-x64-12
          evict-old-files: 1d
          save: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' }}

      # 方案：choco 直接安装CUDA12.8，彻底规避第三方Action版本校验报错
      - name: Install CUDA 12.8 via Chocolatey
        run: |
          choco install nvidia-cuda --version=12.8.0.520 -y
          echo "CUDA_PATH=C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.8" >> $env:GITHUB_ENV
          echo "${env:CUDA_PATH}\bin" >> $env:GITHUB_PATH

      - name: CMake Build CUDA (Enable llama-server & full MCP)
        run: |
          Remove-Item -Recurse -Force build-cuda -ErrorAction SilentlyContinue
          cmake -S . -B build-cuda ^
            -A x64 ^
            -DGGML_NATIVE=OFF ^
            -DLLAMA_BUILD_SERVER=ON ^
            -DLLAMA_BUILD_BORINGSSL=ON ^
            -DGGML_RPC=ON ^
            -DGGML_BACKEND_DL=ON ^
            -DGGML_CPU_ALL_VARIANTS=ON ^
            -DGGML_CUDA=ON ^
            -DGGML_CUDA_CUB_3DOT2=ON
          cmake --build build-cuda --config Release -j %NUMBER_OF_PROCESSORS%

      - name: CTest basic functional test
        run: |
          cd build-cuda
          ctest -L main -C Release --verbose --timeout 900

      - name: Package CUDA release zip
        run: |
          mkdir artifact-cuda
          Copy-Item build-cuda/Release/* artifact-cuda/ -Recurse
          Compress-Archive -Path artifact-cuda/* -DestinationPath llama-win-cuda12-mcp.zip

      - name: Upload CUDA build artifact
        uses: actions/upload-artifact@v4
        with:
          name: llama-win-cuda12-mcp
          path: llama-win-cuda12-mcp.zip
